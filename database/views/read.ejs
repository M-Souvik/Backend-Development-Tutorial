<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/global.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white">   
     <div class="bg-zinc-900 w-full min-h-screen p-10 ">
        <a href="/" class="text-blue-500">Back to homepage</a>
        <h1 class="text-xl font-semibold ">All User</h1>
        <hr/>
        <div class="users flex gap-3 flex-wrap">
            <% users.forEach(user=>{  %>
            <div class="user w-64 bg-zinc-800 mx-3 my-3 px-3 py-3 rounded-lg">
                <div class="bg-zinc-700 h-44">
                    <img src="<%=user.image%>" class="h-full w-full object-cover"/>
                </div>
                <h1 class="text-xl"><%= user.username%></h1>
                <h1 class="text-lg text-gray-600"><%= user.email%></h1>
                <div class="flex gap-4 justify-center py-4">
                    <a href="/edit/<%=user._id%>" class="text-md p-1 rounded-lg text-blue-700 font-bold">Edit User</a>
                    <a href="/delete/<%=user._id%>" class="text-md p-1 rounded-lg text-red-700 font-bold">Delete User</a>
                </div>
            </div>
        </div>
        <%})%>
        </div>
     </div>
</body>
</html>