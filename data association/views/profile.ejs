<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="bg-zinc-900">
    <div class="h-screen w-full p-10">
        <div class="flex justify-end w-full">
            <a href="/logout" class="text-sm font-semibold bg-red-500 rounded-md px-5 py-3 my-4 mr-0">Logout</a>
        </div>
        <div class="rounded-full w-20 h-20 ">
            <img src="/images/uploads/<%= user.profile%>" alt="" class="h-full w-full object-cover overflow-hidden">
        </div>
        <h1 class="text-2xl font-bold text-white mt-2">Welcome,<%=user.username%>👋</h1>
        <h3 class="text-md font-semibold text-white mb-5">You can create post here</h3>
        <form action="/post" method="post">
            <textarea type="text" rows="5" cols="60" class="block bg-transparent border hover:ring rounded-md text-white p-2" placeholder="What's in your mind?" name="postdata"></textarea>
            <input type="submit" value="Submit" class="bg-blue-400 block m-2 px-4 py-3 rounded-md font-semibold"/>
        </form>
        <div class="posts mt-20">
         <h3 class="text-zinc-400 text-2xl font-semibold mb-2">Your Posts</h3>
            <div class="postcontainer m-4">
                <%user.posts.reverse().forEach(function(post){%>
                <div class="post w-1/3 p-5 border-2 border-zinc-800 rounded-lg bg-zinc-800 my-4">
                <span class="text-blue-500 underline"> @<%=user.username%> </span>
                <p class="text-white tracking-tight"><%= post.postdata%></p>
                <span class="text-white font-semibold"><%=post.likes.length%> Likes</span>
                <div class="flex justify-center gap-5 mt-1">
                    <a href="/like/<%=post._id%>" class="text-blue-500">
                        <%=post.likes.indexOf(user._id)===-1 ? "Like👍" : "Unlike👎"%> 
                    </a>
                    <a href="/edit/<%=post._id%>" class="text-gray-500">Edit</a>
                </div>
                </div>
                <%})%>
            </div>
        </div>
    </div>
    

</body>
</html>